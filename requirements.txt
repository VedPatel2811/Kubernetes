# Web Framework
Flask==2.3.2

# Flask Extensions
Flask-Cors==3.0.10  # For handling CORS (if needed)
Flask-RESTful==0.3.9  # For building REST APIs

# YAML Handling (for generating Kubernetes manifests)
PyYAML==6.0

# Request Handling (optional, for calling APIs)
requests==2.31.0

# Kubernetes Client (optional, for direct Kubernetes interactions)
kubernetes==26.1.0

# Development Tools
gunicorn==20.1.0  # Production-ready WSGI server

# Linting and Formatting
flake8==6.1.0
black==23.9.1

# Testing
pytest==7.4.2
pytest-cov==6.0.0

# Optional Database Support (choose as required)
SQLAlchemy==2.0.37  # ORM for relational databases
pymysql==1.1.1  # PostgreSQL adapter (if using PostgreSQL)

# Monitoring/Telemetry (optional)
prometheus-client==0.16.0
