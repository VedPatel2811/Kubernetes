apiVersion: example.com/v1
kind: Processor
metadata:
  name: pdf-processor
spec:
  name: "PDF Processor"
  serviceName: "pdf-processor-service"
  ownerName: "Diana"
  ownerEmail: "diana@example.com"
  runtime: "python3.10"
  tags:
    - "pdf"
    - "processing"
    - "text"
  description: "Processes PDF files to extract text and metadata."
  actsOn: "application/pdf"
  repoURL: "https://github.com/diana/pdf-processor"